import { Button, styled } from '@mui/material';

const ShopNowButton = styled(Button)({
  display: 'flex',
  alignItems: 'center',
  fontSize: '1rem',
  height: '2.8rem',
  width: '8rem',
});

export default ShopNowButton;
