import { Card, styled } from '@mui/material';

/**
 * Uses Material UI Card component.
 * Product preview is used for actual products offered.
 * */
const CustomProductPreview = styled(Card)({
  marginBottom: '1.5rem',
  height: '30.375rem',
  width: '18.376',
});

export default CustomProductPreview;
