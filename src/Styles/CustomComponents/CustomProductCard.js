import { styled, Card } from '@mui/material';

/**
 * Uses Material UI Card component.
 * Used for product type (Earbuds, Headphones, Speakers).
 * */
const CustomProductCard = styled(Card)({
  marginBottom: '1.5rem',
  height: '26rem',
  width: '25.063rem',
});

export default CustomProductCard;
